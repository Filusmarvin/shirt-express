function textInCalcScreen (event) {
  let number = event.target.closest('.calculator');
  let onScreen = number.querySelector('.answer');
  console.log('yay');
  let blank = event.target.innerText;
 onScreen.innerText = blank;
};
// let numbers = document.querySelector('button')
// for (i = 0; i < numbers.length; i++){
//  let number = numbers[i];
// }
addEventListener('click', textInCalcScreen);

// function {
//
// }
While (calculationArray.length) =
let divideIndex =
let multi
